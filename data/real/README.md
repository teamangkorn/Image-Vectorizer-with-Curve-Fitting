# Real-world data

This project did not require a real-world dataset for the scaling
experiment — synthetic images are sufficient because they let us
control the boundary length precisely (see `../synthetic/README.md`).

For ad-hoc qualitative testing we used the bundled `examples/disk.png`
plus user-supplied PNGs of logos and silhouettes, none of which are
checked in to keep the repository small.
